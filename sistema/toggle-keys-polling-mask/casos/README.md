# Casos confirmados — toggle keys atascados

Cada archivo `NNN-<distro>-<hardware>.md` documenta un caso donde el estado lock enmascaró un debug de polling.

Si te ocurre con otro hardware o app, abre un PR con un nuevo caso.
